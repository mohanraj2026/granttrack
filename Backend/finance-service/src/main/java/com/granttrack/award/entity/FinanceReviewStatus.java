package com.granttrack.award.entity;

/** Whether the assigned Finance Officer has accepted the award for disbursement setup. */
public enum FinanceReviewStatus {
    PENDING,
    ACCEPTED,
    REJECTED
}
